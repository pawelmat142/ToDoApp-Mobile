import { ShowWhenDirective } from './show-when.directive';

describe('ShowWhenDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowWhenDirective();
    expect(directive).toBeTruthy();
  });
});
